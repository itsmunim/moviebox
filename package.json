{
  "name": "moviebox",
  "version": "0.0.1",
  "description": "Simple enough yet another media server framework.",
  "main": "index.js",
  "author": "Munim Dibosh",
  "license": "MIT",
  "scripts": {
    "build-server": "rimraf dist/ && babel ./src/server --out-dir dist/server/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./yarn.lock,./.babelrc --copy-files",
    "start-server": "yarn build-server && node dist/server/index.js"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
